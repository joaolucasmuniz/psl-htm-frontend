export type CardProps = {
  id: number,
  name: string,
};

export type TClientDetails = {
  id: number,
  access_level: number,
  address: string,
  email: string,
  name: string,
  phone: string,
  postalcode: string,
};
